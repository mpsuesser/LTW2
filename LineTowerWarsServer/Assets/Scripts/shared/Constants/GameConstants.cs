public class GameConstants {
    public const float IncomeInterval = 10f;
    public const int StartingGold = 100000;
    public const int StartingLives = 50;
    public const int StartingIncome = 10;
    
    // We have this because we want to represent unit move speed in terms of
    // WC3, but also want to keep it in proper scale with the game
    public const float MoveSpeedDivisor = 16;
}
